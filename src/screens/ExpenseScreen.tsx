import React from 'react';
import {Text, View} from 'react-native';

interface ExpenseScreenPropsInterface {}

function ExpenseScreen(props: ExpenseScreenPropsInterface) {
  return (
    <View>
      <Text>ExpenseScreen Screen</Text>
    </View>
  );
}

export default ExpenseScreen;
